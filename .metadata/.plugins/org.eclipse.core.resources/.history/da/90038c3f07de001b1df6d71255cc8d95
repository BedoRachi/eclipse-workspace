package q;

import p.A;

public class B extends A {
	
	void set() {
		pub =1;
		pro =2; // protected는 내부, 외부 Package에서 사용 가능(상속시)
		def =3; // Default는 내부 Package에서만 사용 가능(외부 Package 불가)
		//pri =4; Error 발생, Private은 외부 클래스에서 사용 불가 
	}

}
